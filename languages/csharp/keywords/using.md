# `using`

The `using` keyword allows one to import a [`namespace`][csharp-info-namespaces]. When using types from imported namespaces, these na don't require (unless there is ambiguity due to types with the same name).

[csharp-info-namespaces][../info/namespaces.md]
